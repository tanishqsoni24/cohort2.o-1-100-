import React from "react";

export default function({ children }:{children: React.ReactNode}){
    return (
        <div>
            20% off for both pages page
            {children}
        </div>
    )  
} 